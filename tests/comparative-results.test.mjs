import assert from 'node:assert/strict';
import { buildComparativeResultsReport } from '../scripts/comparative-results.mjs';

const row = { prediction_id: 'p1', created_at_utc: '2026-06-20T10:00:00Z', match_id: 1, stage: 'group', home_team: 'Alpha', away_team: 'Beta', model_version: 'm', data_version: 'd', source_snapshot_hash: 'a'.repeat(64), predicted_wdl_probs: { home_win: .7, draw: .2, away_win: .1 }, predicted_scoreline_distribution: [{ score: '1-0', probability: 1 }], predicted_advancement_probs: {}, actual_home_score: 1, actual_away_score: 0, actual_result: 'home_win', settled_at_utc: '2026-06-20T22:00:00Z', brier_score: .1, log_loss: .2, scoreline_error: 0, calibration_bucket: '0.7-0.8', failure_type: 'pure_variance' };
const knockoutRow = { ...row, prediction_id: 'k1', match_id: 73, stage: 'R32', predicted_wdl_probs: { home_win: .2, draw: .6, away_win: .2 }, predicted_advancement_probs: { home: .3, draw: 0, away: .7 }, actual_home_score: 1, actual_away_score: 1, actual_result: 'draw', scoreline_error: 2, calibration_bucket: '0.6-0.7', failure_type: 'knockout_penalty_variance' };
const report = buildComparativeResultsReport({ ledger: { generated_at_utc: '2026-06-21T00:00:00Z', predictions: [row, { ...row, prediction_id: 'p2', created_at_utc: '2026-06-20T11:00:00Z' }, knockoutRow] }, data: { teams: [{ name: 'Alpha', rank: 10 }, { name: 'Beta', rank: 40 }], matches: [{ no: 1, date: '2026-06-20T18:00:00Z' }], knockout: [{ no: 73, date: '2026-06-20T18:00:00Z', teamA: 'Alpha', teamB: 'Beta', played: true, scoreA: 1, scoreB: 1, winner: 'Beta', loser: 'Alpha' }] }, calibrationState: {}, asOfUtc: '2026-06-21T00:00:00Z' });
assert.equal(report.denominators.settled_eligible_forecasts, 3);
assert.equal(report.denominators.settled_matches, 2);
assert.equal(report.denominators.excluded_duplicate_snapshots, 1);
assert.equal(report.summary.outcome_accuracy.correct, 2);
assert.equal(report.summary.exact_score_accuracy.correct, 1);
assert.deepEqual(report.summary.advancement_accuracy, { correct: 1, count: 1, rate: 1 });
assert.equal(report.rows[0].actual_score, '1–0');
assert.equal(report.rows[0].advancement_correct, null);
assert.equal(report.rows[1].actual_outcome, 'draw');
assert.equal(report.rows[1].predicted_advancing_team, 'Beta');
assert.equal(report.rows[1].actual_advancing_team, 'Beta');
assert.equal(report.rows[1].predicted_advancement_confidence, .7);
assert.equal(report.rows[1].advancement_correct, true);
console.log('comparative-results tests passed');
